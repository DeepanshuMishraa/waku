name: Release

on:
  release:
    types: [published]
  workflow_dispatch:
    inputs:
      tag:
        description: Release tag to build and publish
        required: true
        type: string

permissions:
  contents: write

concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false

env:
  CARGO_TERM_COLOR: always

jobs:
  version:
    name: Resolve version
    runs-on: ubuntu-latest
    timeout-minutes: 5
    outputs:
      version: ${{ steps.resolve.outputs.version }}
      tag: ${{ steps.resolve.outputs.tag }}
    steps:
      - uses: actions/checkout@v6

      - name: Resolve version from Cargo.toml
        id: resolve
        run: |
          version=$(sed -n 's/^version = "\(.*\)"/\1/p' Cargo.toml | head -1)
          if [ -z "$version" ]; then
            echo "::error::No version found in Cargo.toml"
            exit 1
          fi
          tag="${{ github.event.release.tag_name || inputs.tag }}"
          if [[ ! "$tag" =~ ^v[0-9]+\.[0-9]+\.[0-9]+$ ]]; then
            echo "::error::Tag must use the vX.Y.Z format, got $tag"
            exit 1
          fi
          if [ "${tag#v}" != "$version" ]; then
            echo "::error::Tag $tag does not match Cargo.toml version $version"
            exit 1
          fi
          echo "version=$version" >> "$GITHUB_OUTPUT"
          echo "tag=$tag" >> "$GITHUB_OUTPUT"
          echo "Building assets for published release $tag"

#   linux-x86_64:
#     name: Linux x86_64
#     needs: version
#     runs-on: ubuntu-22.04
#     timeout-minutes: 90
#     steps:
#       - uses: actions/checkout@v6

#       - name: Install Rust
#         run: |
#           rustup toolchain install 1.96.0 --profile minimal
#           rustup default 1.96.0

#       - name: Install Linux dependencies
#         run: |
#           sudo apt-get update
#           sudo apt-get install --yes \
#             clang \
#             cmake \
#             libfontconfig-dev \
#             libvulkan1 \
#             libwayland-dev \
#             libx11-xcb-dev \
#             libxkbcommon-x11-dev \
#             pkg-config \
#             xdg-desktop-portal

#       - uses: actions/cache@v6
#         with:
#           path: |
#             ~/.cargo/registry
#             ~/.cargo/git
#             target
#           key: ubuntu-22.04-rust-1.96.0-release-${{ hashFiles('Cargo.lock') }}
#           restore-keys: |
#             ubuntu-22.04-rust-1.96.0-release-

#       # `target/` is restored from the release cache, so the previous
#       # release's bundles are still sitting in `target/release` when this one
#       # starts. Cargo does not own them, so nothing else clears them.
#       - name: Clear stale bundles
#         run: rm -f target/release/insulator-*.tar.gz

#       - name: Bundle
#         run: ./scripts/bundle-linux.sh

#       - uses: actions/upload-artifact@v7
#         with:
#           name: linux-x86_64
#           # Named exactly, not globbed: `if-no-files-found: error` is then a
#           # real check that this version built, rather than something an older
#           # bundle can satisfy.
#           path: target/release/insulator-${{ needs.version.outputs.version }}-x86_64-unknown-linux-gnu.tar.gz
#           if-no-files-found: error

#   linux-arm64:
#     name: Linux arm64
#     needs: version
#     runs-on: ubuntu-22.04-arm
#     timeout-minutes: 90
#     steps:
#       - uses: actions/checkout@v6

#       - name: Install Rust
#         run: |
#           rustup toolchain install 1.96.0 --profile minimal
#           rustup default 1.96.0

#       - name: Install Linux dependencies
#         run: |
#           sudo apt-get update
#           sudo apt-get install --yes \
#             clang \
#             cmake \
#             libfontconfig-dev \
#             libvulkan1 \
#             libwayland-dev \
#             libx11-xcb-dev \
#             libxkbcommon-x11-dev \
#             pkg-config \
#             xdg-desktop-portal

#       - uses: actions/cache@v6
#         with:
#           path: |
#             ~/.cargo/registry
#             ~/.cargo/git
#             target
#           key: ubuntu-22.04-arm-rust-1.96.0-release-${{ hashFiles('Cargo.lock') }}
#           restore-keys: |
#             ubuntu-22.04-arm-rust-1.96.0-release-

#       # `target/` is restored from the release cache, so the previous
#       # release's bundles are still sitting in `target/release` when this one
#       # starts. Cargo does not own them, so nothing else clears them.
#       - name: Clear stale bundles
#         run: rm -f target/release/insulator-*.tar.gz

#       - name: Bundle
#         run: ./scripts/bundle-linux.sh

#       - uses: actions/upload-artifact@v7
#         with:
#           name: linux-arm64
#           path: target/release/insulator-${{ needs.version.outputs.version }}-aarch64-unknown-linux-gnu.tar.gz
#           if-no-files-found: error

#   windows-x86_64:
#     name: Windows x86_64
#     needs: version
#     runs-on: windows-latest
#     timeout-minutes: 90
#     steps:
#       - uses: actions/checkout@v6

#       - name: Install Rust
#         run: |
#           rustup toolchain install 1.96.0 --profile minimal
#           rustup default 1.96.0
#           rustup target add x86_64-pc-windows-msvc

#       - uses: oven-sh/setup-bun@v2

#       - name: Install Inno Setup
#         run: choco install innosetup --yes --no-progress

#       - uses: actions/cache@v6
#         with:
#           path: |
#             ~/.cargo/registry
#             ~/.cargo/git
#             target
#           key: windows-latest-rust-1.96.0-release-${{ hashFiles('Cargo.lock') }}
#           restore-keys: |
#             windows-latest-rust-1.96.0-release-

#       # `target/` is restored from the release cache, so the previous
#       # release's bundles are still sitting in `target/release` when this one
#       # starts. Cargo does not own them, so nothing else clears them.
#       - name: Clear stale bundles
#         run: Remove-Item -Force -ErrorAction SilentlyContinue target/release/insulator-*.zip, target/release/Insulator-*-Setup.exe

#       - name: Bundle
#         env:
#           WINDOWS_CERTIFICATE: ${{ secrets.WINDOWS_CERTIFICATE }}
#           WINDOWS_CERTIFICATE_PASSWORD: ${{ secrets.WINDOWS_CERTIFICATE_PASSWORD }}
#         run: bun scripts/bundle-windows.ts

#       - uses: actions/upload-artifact@v7
#         with:
#           name: windows-x86_64
#           path: |
#             target/release/insulator-${{ needs.version.outputs.version }}-x86_64-pc-windows-msvc.zip
#             target/release/Insulator-${{ needs.version.outputs.version }}-x86_64-Setup.exe
#           if-no-files-found: error

#   windows-arm64:
#     name: Windows arm64
#     needs: version
#     runs-on: windows-11-arm
#     timeout-minutes: 90
#     steps:
#       - uses: actions/checkout@v6

#       - name: Install Rust
#         run: |
#           rustup toolchain install 1.96.0 --profile minimal
#           rustup default 1.96.0
#           rustup target add aarch64-pc-windows-msvc

#       - uses: oven-sh/setup-bun@v2

#       - name: Install Inno Setup
#         run: choco install innosetup --yes --no-progress

#       - uses: actions/cache@v6
#         with:
#           path: |
#             ~/.cargo/registry
#             ~/.cargo/git
#             target
#           key: windows-11-arm-rust-1.96.0-release-${{ hashFiles('Cargo.lock') }}
#           restore-keys: |
#             windows-11-arm-rust-1.96.0-release-

#       # `target/` is restored from the release cache, so the previous
#       # release's bundles are still sitting in `target/release` when this one
#       # starts. Cargo does not own them, so nothing else clears them.
#       - name: Clear stale bundles
#         run: Remove-Item -Force -ErrorAction SilentlyContinue target/release/insulator-*.zip, target/release/Insulator-*-Setup.exe

#       - name: Bundle
#         env:
#           WINDOWS_CERTIFICATE: ${{ secrets.WINDOWS_CERTIFICATE }}
#           WINDOWS_CERTIFICATE_PASSWORD: ${{ secrets.WINDOWS_CERTIFICATE_PASSWORD }}
#         run: bun scripts/bundle-windows.ts

#       - uses: actions/upload-artifact@v7
#         with:
#           name: windows-arm64
#           path: |
#             target/release/insulator-${{ needs.version.outputs.version }}-aarch64-pc-windows-msvc.zip
#             target/release/Insulator-${{ needs.version.outputs.version }}-aarch64-Setup.exe
#           if-no-files-found: error

  macos:
    name: macOS
    needs: version
    runs-on: macos-latest
    timeout-minutes: 90
    steps:
      - uses: actions/checkout@v6

      - name: Install Rust
        run: |
          rustup toolchain install 1.96.0 --profile minimal
          rustup default 1.96.0

      - uses: oven-sh/setup-bun@v2

      - name: Install create-dmg
        run: brew install create-dmg

      - uses: actions/cache@v6
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: macos-latest-rust-1.96.0-release-${{ hashFiles('Cargo.lock') }}
          restore-keys: |
            macos-latest-rust-1.96.0-release-

      # Insulator is distributed without a Developer ID certificate or
      # notarization. Sparkle still authenticates the update archive with its
      # Ed25519 key, while the app and DMG use an ad-hoc signature.
      - name: Release
        env:
          SPARKLE_PRIVATE_KEY: ${{ secrets.SPARKLE_PRIVATE_KEY }}
          INSULATOR_DOWNLOAD_URL_PREFIX: https://github.com/${{ github.repository }}/releases/download/v${{ needs.version.outputs.version }}/
        run: bun run release --local --adhoc

      - uses: actions/upload-artifact@v7
        with:
          name: macos
          path: |
            dist/Insulator-${{ needs.version.outputs.version }}.dmg
            dist/Insulator-${{ needs.version.outputs.version }}.md
            dist/Insulator-${{ needs.version.outputs.version }}.zip
            dist/appcast.xml
          if-no-files-found: error

      - name: Publish macOS assets and release notes
        env:
          GH_TOKEN: ${{ github.token }}
          VERSION: ${{ needs.version.outputs.version }}
        run: |
          bun -e '
            import { extractReleaseNotes } from "./scripts/changelog.ts";
            const notes = extractReleaseNotes(await Bun.file("CHANGELOG.md").text(), process.env.VERSION);
            await Bun.write("release-notes.md", `${notes ?? "See CHANGELOG.md for details."}\\n`);
          '
          gh release edit "${{ needs.version.outputs.tag }}" \
            --repo "${{ github.repository }}" \
            --notes-file release-notes.md
          gh release upload "${{ needs.version.outputs.tag }}" \
            --repo "${{ github.repository }}" \
            --clobber \
            dist/Insulator-${{ needs.version.outputs.version }}.dmg \
            dist/Insulator-${{ needs.version.outputs.version }}.md \
            dist/Insulator-${{ needs.version.outputs.version }}.zip \
            dist/appcast.xml

  macos-x86_64:
    name: macOS x86_64
    needs: version
    runs-on: macos-15-intel
    timeout-minutes: 90
    steps:
      - uses: actions/checkout@v6

      - name: Install Rust
        run: |
          rustup toolchain install 1.96.0 --profile minimal
          rustup default 1.96.0

      - uses: oven-sh/setup-bun@v2

      - name: Install create-dmg
        run: brew install create-dmg

      - uses: actions/cache@v6
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: macos-15-intel-rust-1.96.0-release-${{ hashFiles('Cargo.lock') }}
          restore-keys: |
            macos-15-intel-rust-1.96.0-release-

      - name: Build x86_64 DMG
        env:
          SPARKLE_PRIVATE_KEY: ${{ secrets.SPARKLE_PRIVATE_KEY }}
          INSULATOR_DOWNLOAD_URL_PREFIX: https://github.com/${{ github.repository }}/releases/download/v${{ needs.version.outputs.version }}/
        run: |
          bun run release --local --adhoc
          mv "dist/Insulator-${{ needs.version.outputs.version }}.dmg" \
             "dist/Insulator-${{ needs.version.outputs.version }}-x86_64.dmg"

      - uses: actions/upload-artifact@v7
        with:
          name: macos-x86_64
          path: dist/Insulator-${{ needs.version.outputs.version }}-x86_64.dmg
          if-no-files-found: error

      - name: Publish x86_64 DMG
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh release upload "${{ needs.version.outputs.tag }}" \
            --repo "${{ github.repository }}" \
            --clobber \
            "dist/Insulator-${{ needs.version.outputs.version }}-x86_64.dmg"

#   attach-release-assets:
#     name: Attach assets to GitHub release
#     needs:
#       [
#         version,
#         linux-x86_64,
#         linux-arm64,
#         windows-x86_64,
#         windows-arm64,
#         macos,
#       ]
#     runs-on: ubuntu-latest
#     timeout-minutes: 15
#     permissions:
#       contents: write
#     steps:
#       - uses: actions/checkout@v6

#       - uses: oven-sh/setup-bun@v2

#       - uses: actions/download-artifact@v8
#         with:
#           path: artifacts
#           merge-multiple: true

#       - name: Write the version pointers
#         env:
#           VERSION: ${{ needs.version.outputs.version }}
#         run: |
#           # What `install.sh` and the website's Windows download resolve
#           # "latest" to. In-app updates read the appcasts instead.
#           echo "$VERSION" > artifacts/latest-linux.txt
#           echo "$VERSION" > artifacts/latest-windows.txt

#       # Signed here rather than on platform runners: this is the only job that
#       # holds every native artifact, and the signing key stays in one place.
#       - name: Sign the Linux and Windows update feeds
#         env:
#           VERSION: ${{ needs.version.outputs.version }}
#           SPARKLE_PRIVATE_KEY: ${{ secrets.SPARKLE_PRIVATE_KEY }}
#         run: |
#           # Merge into the live feeds so previously published releases keep
#           # their entries; a first run simply starts from nothing.
#           for platform in linux windows; do
#             for arch in x86_64 aarch64; do
#               curl -fsSL --max-time 30 \
#                 "https://releases.insulator.sh/appcast-$platform-$arch.xml" \
#                 -o "artifacts/appcast-$platform-$arch.xml" || true
#             done
#           done
#           bun scripts/appcast-linux.ts artifacts "$VERSION"
#           bun scripts/appcast-windows.ts artifacts "$VERSION"

#       - name: Write release notes
#         env:
#           VERSION: ${{ needs.version.outputs.version }}
#         run: |
#           bun -e '
#             import { extractReleaseNotes } from "./scripts/changelog.ts";
#             const version = process.env.VERSION;
#             const file = Bun.file("CHANGELOG.md");
#             const notes = (await file.exists())
#               ? extractReleaseNotes(await file.text(), version)
#               : null;
#             await Bun.write(
#               "release-notes.md",
#               `${notes ?? "See CHANGELOG.md for details."}\n`,
#             );
#           '

#       - uses: softprops/action-gh-release@v3
#         with:
#           draft: false
#           tag_name: ${{ needs.version.outputs.tag }}
#           name: ${{ needs.version.outputs.tag }}
#           body_path: release-notes.md
#           fail_on_unmatched_files: true
#           files: |
#             artifacts/*.tar.gz
#             artifacts/*.dmg
#             artifacts/*.md
#             artifacts/*.zip
#             artifacts/*.exe
#             artifacts/appcast.xml
#             artifacts/appcast-linux-*.xml
#             artifacts/appcast-windows-*.xml
#             artifacts/latest-linux.txt
#             artifacts/latest-windows.txt
